# Let NetworkManager manage all devices on this system
network:
  version: 2
  ethernets:
      NETWORK_DRIVER:
          addresses: 
            - IP_ADRESS
          gateway4: GATEWAY_IP
          dhcp4: yes
          nameservers:
              addresses: NAMESERVER_IP
